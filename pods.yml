apiVersion: v1
kind: Pod
metadata:
  name: nodeapp
  labels:
    app: nodeapp
spec:
  containers:
    - name: nodeapp
      image: arunksingh015/kubernet:tagVersion
      ports:
        - containerPort: 8080